discoverytool:
  image: "${IMAGE_NAME}:${IMAGE_VERSION}"
  ports:
    - "8083:8083"
  environment:
    - HOTJARID
    - GAID
    - LOGGING_ENVIRONMENT
  external_links:
    - knowledgebase/elastic:elastic
    - knowledgebase/resourceapi:resourceapi
    - knowledgebase/schema:schema
